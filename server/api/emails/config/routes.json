{
  "routes": [
    {
      "method": "GET",
      "path": "/emails",
      "handler": "emails.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/emails/count",
      "handler": "emails.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/emails/:id",
      "handler": "emails.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/emails",
      "handler": "emails.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/emails/:id",
      "handler": "emails.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/emails/:id",
      "handler": "emails.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
